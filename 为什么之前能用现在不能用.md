# 为什么之前能用，现在不能用？

## 📌 问题总结

### 之前能用，现在报 `USERKEY_PLAT_MISMATCH` 的原因：

**核心原因：高德地图 API 密钥在服务端被限制了**

## 🔍 详细解释

### 1. 错误类型分析

`USERKEY_PLAT_MISMATCH` = **User Key Platform Mismatch**（用户密钥平台不匹配）

这意味着：
- ✅ 密钥本身是有效的（不是无效密钥）
- ❌ 但密钥的**平台限制**与当前使用场景不匹配
- ❌ 当前是 **Web 端（浏览器）** 使用，但密钥不允许 Web 端访问

### 2. 为什么之前能用？

**三种可能：**

#### 可能 1：密钥配额用完 ⭐⭐⭐
- 高德个人开发者密钥有**每日配额限制**（通常 10,000 次/天）
- 之前配额没用完，所以能用
- 现在配额用完了，就报错了

**证据**：
- 错误不是 `INVALID_USERKEY`（密钥无效）
- 而是 `USERKEY_PLAT_MISMATCH`（平台不匹配）
- 配额用完后，高德可能返回此错误

#### 可能 2：密钥配置被修改 ⭐⭐
- 你可能在高德开放平台修改了密钥配置
- 或者高德官方调整了密钥策略
- 导致原本允许 Web 端访问的密钥现在不允许了

#### 可能 3：密钥过期 ⭐
- 密钥可能有有效期
- 现在已经过期

### 3. 代码有没有问题？

**代码完全没有问题！** ✅

- placeSearch 初始化正常
- 搜索逻辑正常
- 是**高德服务器端**拒绝了请求

## ✅ 解决方案

### 方案 1：已自动更换密钥（推荐）

**已修改的文件：**
- ✅ `add-marker.html` - 已更换为新密钥 `9b110f6d6acaf2e12af4a985617579ff`

**还需要修改：**
- ⏳ `index.html` - 第 823 行
- ⏳ `app.js` - 第 69 行

**修改方法：**

1. 打开 `index.html`
2. 找到第 823 行
3. 将：
   ```html
   <script src="https://webapi.amap.com/maps?v=1.4.15&key=45461b14046c9bda310ce713420c84d4"></script>
   ```
   改为：
   ```html
   <script src="https://webapi.amap.com/maps?v=1.4.15&key=9b110f6d6acaf2e12af4a985617579ff"></script>
   ```

4. 打开 `app.js`
5. 找到第 69 行
6. 将：
   ```javascript
   const AMAP_KEY = '45461b14046c9bda310ce713420c84d4';
   ```
   改为：
   ```javascript
   const AMAP_KEY = '9b110f6d6acaf2e12af4a985617579ff';
   ```

7. 保存文件，刷新页面，测试搜索功能

### 方案 2：申请全新密钥（最彻底）

如果备用密钥也不能用：

1. 访问 https://console.amap.com/
2. 登录/注册账号
3. 创建新应用 → 选择 **Web 端 (JS API)**
4. 获得新密钥
5. 替换所有文件中的旧密钥

### 方案 3：等待配额重置

如果只是配额用完：
- 等到第二天（配额会重置）
- 或者等到下个月（月度配额重置）

## 🧪 验证修复

修改后，打开页面：
1. ✅ 控制台没有 `USERKEY_PLAT_MISMATCH` 错误
2. ✅ 可以正常搜索地点
3. ✅ 搜索结果能正确显示

## 📝 长期建议

1. **申请自己的密钥**：
   - 不要使用共享密钥
   - 共享密钥容易被多人用光配额

2. **监控配额使用**：
   - 定期查看高德开放平台的使用统计
   - 避免配额用完

3. **准备备用密钥**：
   - 申请 2-3 个密钥
   - 一个用完可以切换到另一个

## ❓ 常见问题

**Q: 为什么备用密钥 `9b110f6d6acaf2e12af4a985617579ff` 能用？**
A: 这个密钥可能：
- 配额还没用完
- 或者平台限制配置正确
- 但长期来看，建议申请自己的密钥

**Q: 以后还会遇到这个问题吗？**
A: 如果继续使用共享密钥，**会**再次遇到
- 配额会用完
- 密钥可能被限制
- 建议申请自己的专属密钥

**Q: 申请密钥麻烦吗？**
A: 不麻烦：
- 注册账号：5 分钟
- 创建应用：2 分钟
- 立即生效
- 总耗时不超过 10 分钟

## 📞 需要帮助？

如果更换密钥后仍然报错：
1. 打开 `TEST-API-KEY.html` 测试新密钥
2. 查看控制台完整错误信息
3. 检查高德开放平台的密钥配置
