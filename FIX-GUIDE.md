# 修复理想充电站地址显示问题

## 问题症状
- 理想充电站信息窗口显示"位置：加载中..."
- 一直没有加载出详细地址

## 原因
逆地理编码 API 调用可能因为网络、缓存或 API 限制而失败

## 解决方案

### 步骤 1：清理浏览器缓存
1. 按 `Ctrl + Shift + Delete` 打开清除浏览历史记录
2. 选择"缓存的图片和文件"
3. 点击"清除数据"

### 步骤 2：清理旧的充电站数据
1. 打开文件：`e:\HFmast\map-app\clean-and-rebuild.html`
2. 点击"清理理想充电站"按钮
3. 确认清理成功

### 步骤 3：重新添加充电站
1. 点击"打开批量添加页面"按钮
2. 在新页面中点击"批量添加理想充电站"
3. 等待添加完成

### 步骤 4：测试地址显示
1. 打开文件：`e:\HFmast\map-app\index.html`
2. 点击任意一个理想充电站标记
3. 应该能看到详细的地址信息

## 调试方法

如果还是显示"加载中..."，请按 F12 打开浏览器控制台，查看：

### 应该看到的日志：
```
正在调用逆地理编码 API: https://restapi.amap.com/v3/geocode/regeo?key=...
逆地理编码 API 返回：{status: "1", regeocode: {...}}
获取到格式化地址：广东省深圳市宝安区 XXX
```

### 可能看到的错误：
1. **网络错误**：`Failed to fetch`
   - 解决：检查网络连接，确保能访问高德地图 API

2. **API 返回错误**：`{status: "0", info: "INVALID_USER_KEY"}`
   - 解决：检查 API Key 是否正确

3. **跨域错误**：`Access to fetch blocked by CORS policy`
   - 解决：使用本地服务器运行，不要直接打开 HTML 文件
   - 推荐使用 VS Code 的 Live Server 插件

## 备用方案

如果逆地理编码 API 持续失败，代码会自动显示坐标信息：
```
纬度：22.554900, 经度：113.887400
```

这是正常的降级处理，确保信息窗口能正常显示。

## 联系支持

如果问题仍未解决，请提供：
1. 浏览器控制台的完整错误信息
2. 截图显示问题
3. 使用的浏览器版本
