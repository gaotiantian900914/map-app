# API 密钥错误修复指南

## 问题描述

搜索功能报错：`USERKEY_PLAT_MISMATCH`

## 错误原因

当前使用的高德地图 API 密钥 `45461b14046c9bda310ce713420c84d4` 被设置了**平台限制**，不允许 Web 端访问。

## 解决方案

### 方案一：更换 API 密钥（推荐）

1. **获取新的 API 密钥**：
   - 访问 [高德开放平台](https://console.amap.com/)
   - 登录/注册账号
   - 进入"应用管理" → "我的应用"
   - 点击"创建新应用"
   - 创建 Web 端密钥（注意：平台选择 **Web 端 (JS API)**）
   - 复制新生成的密钥

2. **替换密钥**：
   - 打开 `index.html` 文件
   - 找到第 823 行：
     ```html
     <script src="https://webapi.amap.com/maps?v=1.4.15&key=45461b14046c9bda310ce713420c84d4"></script>
     ```
   - 将 `key=45461b14046c9bda310ce713420c84d4` 替换为你的新密钥
   
   - 打开 `add-marker.html` 文件
   - 找到第 638 行，同样替换密钥
   
   - 打开 `app.js` 文件
   - 找到第 69 行：
     ```javascript
     const AMAP_KEY = '45461b14046c9bda310ce713420c84d4';
     ```
   - 替换为你的新密钥

3. **更新所有引用该密钥的文件**：
   - 使用文本编辑器的全局搜索功能，搜索 `45461b14046c9bda310ce713420c84d4`
   - 全部替换为你的新密钥

4. **提交到 GitHub**：
   ```bash
   git add .
   git commit -m "修复：更换高德地图 API 密钥"
   git push
   ```

5. **清除浏览器缓存并测试**：
   - 打开 GitHub Pages 在线版本
   - 按 Ctrl+Shift+R 强制刷新
   - 尝试搜索功能

### 方案二：修改现有密钥配置

如果你不想更换密钥，可以修改现有密钥的平台限制：

1. 访问 [高德开放平台](https://console.amap.com/)
2. 进入"应用管理" → "我的应用"
3. 找到使用密钥 `45461b14046c9bda310ce713420c84d4` 的应用
4. 点击"设置"或"编辑"
5. 在"平台限制"中，勾选 **Web 端 (JS API)**
6. 保存设置
7. 等待 5-10 分钟让配置生效

## 临时测试

如果你想快速测试，可以使用备用密钥 `9b110f6d6acaf2e12af4a985617579ff`：

1. 打开 `index.html`
2. 将第 823 行改为：
   ```html
   <script src="https://webapi.amap.com/maps?v=1.4.15&key=9b110f6d6acaf2e12af4a985617579ff"></script>
   ```
3. 刷新页面测试

**注意**：这个密钥可能也有使用限制，建议申请自己的密钥。

## 代码改进

最新的代码已经添加了错误处理：
- ✅ 自动检测 `USERKEY_PLAT_MISMATCH` 错误
- ✅ 显示友好的错误提示
- ✅ 如果 placeSearch 未初始化，会尝试重新初始化
- ✅ 详细的控制台日志帮助诊断问题

## 验证修复

修复后，搜索功能应该：
1. ✅ 可以正常搜索地点
2. ✅ 搜索结果能正确显示在地图上
3. ✅ 控制台没有 `USERKEY_PLAT_MISMATCH` 错误

## 常见问题

**Q: 为什么无痕模式也报错？**
A: 因为这是 API 密钥配置问题，不是浏览器缓存问题。密钥的平台限制是服务器端验证的。

**Q: 申请新密钥需要多长时间？**
A: 立即生效，注册账号后马上可以创建密钥。

**Q: 密钥有使用限制吗？**
A: 高德个人开发者账号的密钥有每日配额限制（通常为 10,000 次/天），个人使用足够。

## 需要帮助？

如果按照上述步骤仍然无法解决，请：
1. 截图控制台的完整错误信息
2. 检查高德开放平台控制台的密钥配置
3. 确认密钥已正确替换到所有文件中
